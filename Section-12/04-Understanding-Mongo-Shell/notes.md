Understanding Mongo Shell

Mongo Shell (mongosh) is a command-line tool used to interact with MongoDB.
It allows us to execute MongoDB commands directly from the terminal.
We can create databases, collections, and perform CRUD operations using the shell.

Keyword: MongoDB Command Line Interface (CLI)
